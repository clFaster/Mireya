<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:installLocation="auto">
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

	<!-- Android TV devices have no touchscreen; declaring it not required lets the app
	     install and run on both TV and regular devices. Leanback is likewise optional. -->
	<uses-feature android:name="android.hardware.touchscreen" android:required="false" />
	<uses-feature android:name="android.software.leanback" android:required="false" />

	<!-- Digital-signage servers are commonly reached over plain HTTP on a LAN, so
	     cleartext traffic must be permitted. -->
	<application
		android:label="Mireya"
		android:icon="@drawable/Icon"
		android:banner="@drawable/banner"
		android:usesCleartextTraffic="true" />
</manifest>
